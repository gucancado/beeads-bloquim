Segue o prompt ajustado para focar em robustez sem depender de testes automatizados:

faça os ajustes finais na implementação de título de tarefas de aprovação antes de concluir a task
não é necessário criar ou rodar testes automatizados. eu farei a validação manual
objetivo:
garantir consistência, segurança e evitar inconsistências futuras
1) backend
- bloquear edição de título em tarefas de aprovação:
  - ao receber PATCH /tasks/:id com alteração de title
  - se a tarefa for de aprovação, retornar erro 400
  - mensagem: "não é permitido alterar o título de tarefas de aprovação"
2) banco de dados (validação, não alteração estrutural)
- verificar e me informar:
  - se parentTaskId possui constraint de foreign key
  - qual o comportamento da FK (CASCADE, SET NULL, RESTRICT)
  - se existe índice em parentTaskId
- não alterar schema, apenas validar e reportar
3) consistência de dados no backend
- garantir que todas as rotas retornem o mesmo nome de campo para o título da tarefa pai:
  - padronizar para: parentTaskTitle
- no caso do endpoint de mapas (cards):
  - manter compatibilidade interna se necessário
  - mas garantir que o frontend receba sempre um campo consistente
4) fallback seguro no frontend
- garantir que nunca seja exibido título vazio
ordem de fallback obrigatória:
- parentTaskTitle (quando existir)
- title da tarefa com remoção do prefixo "aprovação:"
- string padrão: "tarefa sem título"
5) refatoração do canvas
- extrair uma função única para mapear dados do card para o nó de aprovação
  - exemplo: mapApprovalCardToNodeData(card)
- substituir os 3 pontos atuais por essa função
- garantir que todos usem exatamente a mesma lógica
6) auditoria rápida
- verificar se existe algum outro ponto no frontend onde:
  - tarefas de aprovação ainda usam diretamente task.title
- se existir:
  - corrigir para usar o helper getApprovalDisplayTitle
7) resposta final
- listar objetivamente:
  - o que foi alterado
  - o que foi verificado
  - qualquer limitação que ainda exista
não marcar como concluído automaticamente — aguardar minha validação manual